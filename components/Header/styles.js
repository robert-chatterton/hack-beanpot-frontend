import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    topBar: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'top',
    },
    titles: {
        marginTop: '30%',
        width: '100%',
        alignItems: 'center'
    },
    title: {
        fontSize: 40,
        fontWeight: 600
    }
});

export default styles;
